# indyhax-curl

Use curl (or a raw HTTP request) to retrieve something not linked from the UI.

## Running locally
Open index.html.

## GitHub Pages
This repo is intended to be published via GitHub Pages.
